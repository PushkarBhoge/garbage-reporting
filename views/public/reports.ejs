<%- include('../partials/header') %>

<div class="container my-5">
    <h2><%= t('Garbage Reports') %></h2>
    
    <% if (success) { %>
        <div class="alert alert-success"><%= t('Report submitted successfully!') %></div>
    <% } %>
    
    <% if (reports.length === 0) { %>
        <div class="alert alert-info"><%= t('No reports found.') %></div>
    <% } else { %>
        <div class="row">
            <% reports.forEach(report => { %>
                <div class="col-md-6 col-lg-4 mb-4">
                    <div class="card">
                        <img src="<%= report.beforeImage.url %>" class="card-img-top" alt="Before Image" style="height: 200px; object-fit: cover;">
                        <div class="card-body">
                            <h5 class="card-title">
                                <span class="badge bg-<%= report.status === 'Pending' ? 'warning' : report.status === 'Assigned' ? 'info' : 'success' %>">
                                    <%= t(report.status) %>
                                </span>
                            </h5>
                            <p class="card-text">
                                <strong><%= t('Location:') %></strong> <%= report.location.area %>, <%= report.location.landmark %><br>
                                <strong><%= t('City:') %></strong> <%= report.location.city %> - <%= report.location.pincode %><br>
                                <strong><%= t('Description:') %></strong> <%= report.description %>
                            </p>
                            <small class="text-muted"><%= t('Reported on:') %> <%= new Date(report.createdAt).toLocaleDateString() %></small>
                            
                            <% if (report.status === 'Cleaned' && report.afterImage) { %>
                                <hr>
                                <h6><%= t('After Cleaning:') %> 
                                    <button type="button" class="btn btn-success btn-sm ms-2" onclick="viewImage('<%= report.afterImage.url %>')">
                                        <i class="fas fa-eye"></i> <%= t('View') %>
                                    </button>
                                </h6>
                                <% if (report.cleanedAt) { %>
                                    <small class="text-muted"><%= t('Cleaned on:') %> <%= new Date(report.cleanedAt).toLocaleDateString() %></small>
                                <% } %>
                            <% } %>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    <% } %>
</div>

<!-- Image View Modal -->
<div class="modal fade" id="imageViewModal" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title"><%= t('View Image') %></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body text-center">
                <img id="modalImage" src="" class="img-fluid" alt="Image">
            </div>
        </div>
    </div>
</div>

<script>
function viewImage(imageUrl) {
    document.getElementById('modalImage').src = imageUrl;
    new bootstrap.Modal(document.getElementById('imageViewModal')).show();
}
</script>

<%- include('../partials/footer') %>